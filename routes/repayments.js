import express from 'express'

const router = express.Router()

// Placeholder repayment route
router.get('/', (req, res) => {
  res.json({ message: 'Repayments API placeholder' })
})

export default router
